# Release test-to-branch-effect map

This map names the observable effect protected by each release check. A test is
not credited merely because it executes a branch. Curriculum checks resolve
skills through canonical manifest fields or declared capabilities, never
through a historical skill code, title fragment, or fixed catalog size. The
mutation runner separately proves representative families are non-vacuous.

| Test | Protected branch effect |
|---|---|
| `CORE-00` | The canonical curriculum manifest parses, validates, and satisfies its declared counts, ordering, references, and acyclic prerequisite graph. |
| `MANIFEST-CLOSED` | Unknown fields at the manifest root or in localization, licensing, authorship, review, phase, policy, schema, band, level, rationale, source, benchmark, skill, assessment, or constraint records are rejected instead of silently entering the public release contract. |
| `CORE-01` | Duplicate or moved engine markers, or any change to marked engine bytes, changes extraction or SHA-256. |
| `CORE-02` | Removing a stable public API entry point makes the engine contract incomplete. |
| `CORE-03` | Adding a prohibited ambient identifier makes deterministic-engine purity fail. |
| `BEH-01` | The runtime manifest object, version, ordered levels and skills, and exact manifest SHA-256 match the canonical versioned artifact. |
| `BEH-02` | Every declared `generatorProfile` produces deterministic, distinct, answerable work through at least one manifest-selected skill. |
| `BEH-03` | Each runtime skill preserves the manifest's prerequisites, mastery role, CPA phases, representation, family, profile, and constraints. |
| `BEH-04` | Adult, daily, fatigue, and time truncation change the effective limit and stop classification independently of a natural session. |
| `BEH-05` | Every stop path retains exactly one `NON_EVIDENCE` capstone and completes any active re-teach first. |
| `BEH-06` | The manifest band's configured pick cadence is applied and every offered card is meaningfully distinct while preserving the same learning obligation. |
| `BEH-07` | Every declared selection or construction method can physically enter a correct generated answer, including model-based and exact rational or decimal targets where assigned. `STRATEGY_BUILD` independently derives the governed method, ordered intermediate work, and result from the visible task contract for all six affected skills and all 17 task/prompt variants; both self-selected MQ-095 methods grade, missing or altered work fails, the exact `{strategy,work,value}` object is closed, and forged hidden `params.strategy` or `question.answer` data cannot become an oracle. |
| `BEH-08` | Mastery, re-teaching, demotion, gateway pull-back, and promotion branches remain independently observable under the locked progression rules. |
| `BEH-09` | Hint use, deliberate answer change, first-answer miss, and invalid telemetry each leave the clean-feedback branch. |
| `BEH-10` | Repeated seeded generation is byte-equivalent and input state remains unchanged. |
| `BEH-11` | A network API or remote runtime resource in the shipped page is rejected. |
| `BEH-12` | Export and import preserve canonical schema-3 state, including the neutral placement contract, after projecting only the approved import-time placement-draft generation advance; that generation advances exactly once, and the exact manifest identity and digest remain bound. |
| `BEH-13` | Port 8771, the closed `math-quest-local-server:v2` health contract, protected gameplay storage `math-quest:progress:v2`, constant non-PII cutover guard `math-quest:progress:v2:beta1-migration-guard:v1`, and read-only Beta 1 migration key `math-quest:v2` remain distinct; the launcher binds only to loopback and reuses a listener only when its exact normalized-root SHA-256 and canonical served-route payload SHA-256 match the current folder. The dedicated launcher audit also proves that a foreign root and a post-start byte mutation fail closed. |
| `BEH-14` | Unknown child-string records, prompt-template drift, customary measurement, or non-Canadian money in governed contexts is rejected. |
| `BEH-15` | Forbidden rewards, traits, and superlatives fail only in scoped child-facing categories. |
| `BEH-16` | Removing a required machine-checkable model from any applicable manifest-selected capability identifies the affected skill. |
| `BEH-17` | Removing a required governance, neutral research, provenance, privacy, release, machine-readable agent-collaboration, certification-cadence, or finished-work artifact fails the governed-artifact check. Weakening the permanent holistic-certification policy's lifetime authority, quality scope, focused development coverage, defect-regression rule, explicit product/test/harness/pending-gate classification, obsolete-assertion equal-or-stronger-coverage rule, no-green-by-weakening rule, modularity rule, fail-closed gate, frozen-candidate timing, publication authorization, immutable commit/payload binding, post-change full restart, early-run owner approval, or formal-completion obligation also fails. The collaboration policy separately locks its three-agent cap, no-recursion and single-writer rules, bounded review/verification rounds, read-only reviewer default, authoritative-record rule, takeover handoff, and one-cycle owner-opted reviewer exception. The finished-work policy separately locks its one human authority, exact section hash, six approved clauses, truthful completion states, evidence boundaries, scope-change approval, and predefined finish line. |
| `BEH-18` | Equal finite decimals normalize exactly, malformed forms fail, and manifest-selected decimal operations remain reachable from their assigned input method. |
| `BEH-19` | Adult item cap, adult time cap, and remaining daily allowance each constrain and survive save round-trip. |
| `BEH-20` | Duplicate samples do not strengthen mastery; `NON_EVIDENCE` does not revoke fast track. |
| `BEH-21` | An unsolid prerequisite blocks ordinary practice while every level declared by the manifest remains previewable. |
| `BEH-22` | Active level re-teaching blocks promotion and cannot clear without new recovery evidence. |
| `BEH-23` | A rapid clean selection resets the guessing-like streak without offering a fatigue stop. |
| `BEH-24` | Normalized duplicate or exhausted feedback pools fail their capacity branch. |
| `BEH-25` | Any approved child-facing string record change changes its canonical SHA-256. |
| `BEH-26` | Preview application preserves earned level and all skill evidence. |
| `BEH-27` | Unknown, non-finite, malformed, old-schema, or wrong-manifest imports reject without mutating live state. Reset and import advance beyond current, imported, and surviving-draft generation floors; maximum-safe-integer exhaustion rejects without mutating inputs. |
| `BEH-28` | Candidate play days advance or stay equal, but clock rollback cannot lower `maxSeenPlayDay`. |
| `BEH-29` | Starting-point placement begins by returning a cloned state whose private non-PII `runNonce` is incremented and whose derived seed is fresh; the input remains unchanged. The run validates only against that exact committed baseline, resumes deterministically from the nonce/seed and exact `correct` / `incorrect` / `not-sure` answers, rejects stale or unfinished prior-contract runs, and exposes a pure universal Not sure submission. Recommendation and confirmed state preserve separate response counts and `STANDARD` versus `LIMITED_ABSTENTION` confidence; Not sure follows the conservative branch without becoming incorrect feedback. Applying preserves settings, logs, recovery and all prior learning evidence; marks only genuinely unseen, evidence-free skills below the chosen start as `PLACED`; creates no mastery day, mastery contract, witness, or evidence; and schedules at most one deterministic strand-balanced review per skipped level, capped at 20 and staggered by play day. `PLACED` satisfies prerequisites but not mastery, promotion, or capstone eligibility. Ordinary mastery evidence replaces it with `SOLID`; a failed review removes it without lowering the earned level; and a failed placed gateway schedules a broader same-level recheck and pull-back. Application advances beyond a surviving-draft floor, while exhaustion returns an unchanged failure. Exact version-1 records migrate transactionally; records labelled version 2 must contain their version-2 nonce, response-count, and confidence fields or fail closed. Former fake-solid records migrate without fabricating or erasing retained evidence. |
| `BEH-30` | Math Quest Free Play stays hidden until each exact mapped skill has a completed ordinary introduction (`PRACTISING` or `SOLID`). `UNSEEN`, `LEARNING`, `PLACED`, preview, and unrelated skills do not unlock it; Balance Bay requires both `MQ-021` and `MQ-023`. The acquisition matrix proves that counters are the only initial tool, parts require `MQ-011`, numeral range 6–10 requires `MQ-019`, and five-frame sources require `MQ-020`. Activity rounds are deterministic from an injected private play seed, preserve five-frame targets at 1–5 while numeral targets can extend to 10, alternate family roles, expose mathematical equality separately from activity completion, accept exact distinct constructions, reject duplicate all-ones Balance constructions and invalid pieces, and leave the canonical exported learning state byte-identical. |
| `BND-01` | Seven clean results in the latest 12 activates level re-teaching; eight does not. |
| `BND-02` | Exact 0.80 promotion succeeds with solid gateways and fails with one gateway unsolid. |
| `BND-03` | A selection change at 599 ms is ignored; 600 ms is deliberate. |
| `BND-04` | Every spacing-index advance, reset, final repeat, and overdue case is based on the actual review day. |
| `BND-05` | Structurally eligible non-clean evidence revokes fast track; `NON_EVIDENCE` does not. |
| `BND-06` | Every configured stop classification finishes active re-teach and retains a capstone. |
| `BND-07` | Exact rational equivalence, positive denominators, canonical zero, decimal notation, and manifest-required simplest fraction or mixed-number forms disagree at the intended boundaries. |
| `BND-08` | Initial-stage immediate, later same-session, and chronic 3-of-6 across two-session re-teach thresholds are inclusive. |
| `BND-09` | Rapid-selection fatigue uses each stage's inclusive threshold; threshold plus one millisecond does not signal. |
| `BND-10` | The adaptive binary bracket recommends every possible starting level 1 through 21 deterministically in 15–18 questions, within the approved 10–20 range. Exhaustive threshold-edge coverage runs all 42 recommendation/vote-shape routes; every recommendation is reached by both an incorrect-vote and a Not-sure-vote shape, and terminal counts are exactly 15 or 18. Each sampled level uses three different skills and needs two correct responses to move upward. Thirty-six single-deviation routes prove that one incorrect response and one Not sure at each of the 18 all-success positions cannot trap an otherwise successful run below Level 21. A final three-skill verification independently establishes Level 1 when warranted or falls back to the last boundary actually passed, never an untested level. The traversal reaches the exact governed set of 29 input methods, rejects malformed response kinds and reordered or surplus answers, keeps the first two checkpoint strands distinct whenever available, and proves all completed routes have unique child-visible semantic task signatures. Question selection performs at most 32 deterministic primary attempts plus 16 deterministic fallback attempts and fails closed if no distinct task is possible. |

## Finished-work governance effect map

`audit/tests/finished-work-policy.test.mjs` contains exactly six focused,
effect-sensitive governance tests. They bind the one marked human authority to
the closed machine-readable mirror and exercise invalid mutants rather than
accepting matching prose or file presence alone. The focused development entry
point and the final release entry point both execute this same module.

| Test | Protected finished-work effect |
|---|---|
| Unique human authority and exact hash | The two markers occur exactly once and in order; the intervening valid UTF-8 bytes contain the approved product, privacy, completion, pending-evidence, and finish-line promises; and their SHA-256 equals the mirror. |
| Closed six-clause mirror | Only the approved root, scope, five completion states, six ordered clauses, 33 required assertions, authority identity, and hash contract are accepted. |
| Essential mutation rejection | Extra authority, inferred gate weakening, premature completion, missing-evidence certification, unpublished `shipped` status, delivered blockage, deleted clause assertions, a non-normative Markdown source, or a forged section digest each fails independently. |
| Certification-cadence compatibility | `implemented` cannot become formal completion; `release-certified` retains the exact frozen-candidate complete-gate requirement; cadence mutants permitting development completion, removing freeze, or rerunning only affected release checks fail. |
| Registration and execution | The JSON mirror and focused regression must remain in the first-party register and both public inventories, and the shared audit entry point must execute the policy test before its development-only return. |
| Governing-guidance consistency | Contributor, open-source, development, release, and repository guidance must link to the one live heading and preserve truthful status, equal-or-stronger test maintenance, no-ad-hoc-status, and no-automatic-child-data language. |

## Agent-collaboration governance effect map

`audit/tests/agent-collaboration-policy.test.mjs` contains exactly four focused
governance tests. They keep the human authority, closed JSON mirror, legacy
release plan, public inventories, and shared audit entry point consistent.

| Test | Protected collaboration effect |
|---|---|
| Closed bounded policy | The exact default cap, no-recursion and one-writer rules, task packet, peer communication, independent editing, reviewer independence, risk triggers, durable facts, finding fields, takeover fields, one review/verification cycle, optional six-reviewer exception, and workflow cannot be silently weakened. |
| Human authority | The live prose preserves the same caps, ownership, direct communication, independent review, read-only inspection, traceable findings, takeover handoff, termination, and one-cycle exception. |
| Legacy-plan reconciliation | The optional six-reviewer plan cannot create preliminary and final cohorts, and old three-pass or pre-final complete-audit loops cannot override the single frozen-candidate certification cadence. |
| Registration and execution | The JSON mirror and regression stay in the component register and both public inventories, and the shared focused/release entry point executes the regression before its development-only return. |

## Manifest semantic effect map

The semantic suite contributes exactly 130 result records: one load/binding
check, one taxonomy check, one check for each of 126 skills, and two
task-type/mastery checks. A green release run must also reconcile 126 skills,
166 declared task types, and 6,048 deterministic generated questions.

| Test | Protected semantic effect |
|---|---|
| `SEM-CORE` | The shipped engine and canonical manifest both load, and the embedded manifest bytes and SHA-256 exactly match the canonical artifact. |
| `SEM-TAXONOMY` | Every skill has identical manifest generation task types, manifest mastery task types, and runtime task types; the explicit 126-skill semantic signature table is complete. |
| `SEM-MQ-001` … `SEM-MQ-126` | For the named skill, both tiers across the deterministic ordinal cycle cover every declared task type and only its approved prompt/model signature; generation repeats exactly, prompt slots resolve, input and model descriptors are truthful, the answer self-grades, and answer submission preserves the task type. The facet checks additionally require both tenths/hundredths, all four remainder interpretations, both unit-cube/layer volume representations across the declared units and solids, and both 12-/24-hour timetable formats. |
| `SEM-MASTERY-COVERAGE` | A skill cannot become `SOLID` until clean, otherwise-qualified evidence witnesses every required task type and every declared CPA phase in the declared order. |
| `SEM-INVALID-TASK-TYPE` | An undeclared attempt task type is rejected with `REJECTED_ATTEMPT_TASK_TYPE` and cannot append evidence. |
| `node:test` semantic-module import | Native coverage dynamically imports the semantic suite, verifies its exported runner, and requires the exact 130/126/166/6,048 result totals before accepting the engine coverage run. |
| `SAVE-VALID` / `SAVE-REJECT` | Native coverage accepts valid question, construction, pick, feedback, capstone, fatigue, legacy, inactive, and placement-provenance snapshots, then independently rejects every malformed root field, skill record, settings field, placement field, placement-confirmation field, active-session field, queue-slot field, question field, attempt field, and resumable-UI discriminator. Placement rejection includes missing or unknown contracts and fields, invalid applied boundaries, duplicate, unknown, or selected-level placed skill IDs, future confirmation days, invalid recommendation/chosen/applied relationships, and question counts outside 10–20. The exact set of `PLACED` skill records must equal `placedSkillIds`; a placed record cannot carry a mastery day, mastery contract, mastery witness, recovery state, or review date beyond its 20-day placement-review horizon. Ordinary skill spacing remains capped at 12 days. A regular queue cannot contain skills above its active level; a preview queue is bound exactly to its selected preview level. Stage practice, planned-question, time, adult-reduction, queue-length, base-slot, served-count, served-ordinal, and maximum-seen-play-day relationships must all remain internally possible. Persisted questions and ordered pick candidates must reproduce byte-for-byte from their session seed, world, queue slot, and special re-teach/capstone context, so another self-consistent seed, swapped labels, altered prompts, or a mismatched one-more capstone cannot retain untouched hidden answers. Active feedback must regrade the saved response, bind the complete attempt and evidence record, and exactly reproduce the latest approved deterministic feedback-history line. Expired active work is discarded before strict current-question validation while progress/history is preserved and the caller's object remains unchanged; same-day corruption rejects. A prior `question-generator-v5` active question is treated as legacy under v6: only the paused session is discarded while progress and histories survive. A nested invalid snapshot also proves transactional import and throwing export without mutating live state. |
| `API-PARSE` / `API-GRADE` / `API-SUBMIT` | Public parsing, grading, and submission APIs distinguish malformed, equivalent, wrong, normalized, defaulted, reduced-choice, named-candidate, construction, rapid, and invalid-telemetry inputs by asserting their exact canonical result, evidence class, feedback class, or telemetry status. |
| `API-PROGRESSION` / `API-FATIGUE` | Public spacing, fast-track, level re-teaching, promotion, repeated-miss, fatigue, stop-classification, feedback, and child-string branches are checked through exact state, signal, target, and returned-text effects. |
| `API-STATE` | Defensive load/apply/queue paths assert rejected-attempt effects, non-evidentiary isolation, task-type fallback or rejection, cap enforcement, gateway pull-back, restoration, promotion, level re-teach clearing, due-review representation, preview isolation, and empty-session results. |
| `node:test` cross-CPA/world generator matrix | Every declared skill preserves the same task, answer, option values, and mathematical obligation across its applicable CPA representations and all three worlds over a deterministic multi-seed matrix; each generated contract, model, construction self-grade, and distractor effect remains valid. |
| `node:test` public generator/contract failure matrix | Missing, unknown, out-of-domain, answer-bearing, duplicate, ambiguous, undeclared, malformed, and method-mismatched questions fail closed with their specific public contract reason; neutral-world prompt fallbacks remain child-readable. |
| `node:test` structured response grading and persistence matrices | Every structured child input method has a resumable empty state and an exact correct serialization; malformed, boundary, duplicate, out-of-range, forged-history, incomplete, and valid-but-wrong actions remain distinguishable. Invalid persisted responses fail validation and transactional import, except the explicit legacy grid-route migration, which repairs the response and marks the load migrated. |
| `node:test` grid-route geometry matrix | Text, object, coordinate, and default route origins resolve exactly; malformed moves, invalid coordinates, off-board plans, unsafe sizes, excess moves, and traces outside the configured board fail closed. |
| `node:test` active-session binding matrix | Preview, level, fatigue, ordinary, pick, capstone, and re-teach snapshots remain bound to their exact queue, phase, ordinal, candidate resolution, and progress context; cross-phase or cross-progress snapshots fail validation and cannot be exported. |
| `node:test` placement failure matrix | Placement creation, validation, answer submission, completion, and application reject malformed, stale, unsupported, exhausted, or non-transactional requests while preserving the exact live state; a valid conservative completed run still applies only its permitted recommendation. |
| Native coverage calibration and threshold | A full two-way fixture must report 100%, a partial fixture must report less than 100%, and two complementary evaluations of identical bytes under one virtual filename must aggregate to 100%. The exact staged engine URL, root source span, identical V8 range map, nonzero branch total, and exact engine hash are required before the owner-approved minimum of 88% calibrated native branch coverage can gate release. The 88% threshold is a product-owner policy revision recorded on 2026-07-29; it does not relax calibration, exact-byte identity, mutation, or any other release gate. |

## Exhaustive generator effect map

The exhaustive gate evaluates 72,576 deterministic question instances across
all 126 skills, both tiers, every declared task type, and a broad seed/ordinal
matrix. It also performs 40,824 independent question-choice searches. In the
current Beta 2 review, 37,899 searches produced valid distinct pairs and 2,925 were
legitimately suppressed because the learning obligation had no meaningfully
different second sample.

| Check | Protected generator effect |
|---|---|
| Deterministic replay | Identical inputs reproduce byte-equivalent questions, while the audited seed/ordinal matrix reaches every declared task type. |
| Self-grade and input reachability | Every generated correct answer passes the shipped grader and can be entered through the assigned selection or construction control. |
| Constraint and model truth | Generated values, prompt slots, answers, and semantic model descriptors remain within the skill's closed manifest constraints and describe the same mathematical object. |
| Question-choice search | A two-card choice appears only when two distinct samples preserve one obligation; invariant tasks suppress a false choice. |
| Answer-position distribution | The correct slot uses a context-hashed, low-discrepancy 32-bit Weyl sequence for two- and four-option work and a context-rotated base-3 digit sequence for three-option work; each sufficiently sampled task/prompt/arity context uses the required number of positions and no position exceeds 75% of its samples. |

## Browser effect map

The passing browser suite has exactly 72 result records: `BR-01` through
`BR-36`, followed by the 36 visual/profile checks listed in the next section.
`BR-00` is emitted only when the harness itself throws and is never counted in
a passing run.

| Test | Protected browser effect |
|---|---|
| `BR-01` | The shipped game document loads in an installed browser. |
| `BR-02` | The browser executes the shipped deterministic engine object. |
| `BR-03` | Every stable engine entry point is callable through the browser-effects adapter. |
| `BR-04` | Schema-3 progress under `math-quest:progress:v2` and an optional anonymous first-use choice persist independently. The browser fixture writes a real exact schema-2 legacy object under `math-quest:v2`, excluding both current-only `placement` and `placementDraftGeneration` fields; the launched adapter writes schema 3 under the protected-key lease with the current neutral placement contract and generation zero, preserves the migrated progress projection and the legacy source bytes exactly, isolates a later stale schema-2 writer, and leaves private pre-beta progress untouched. |
| `BR-05` | The child-facing application landmark renders. |
| `BR-06` | Enabled, focusable controls render and every one has an accessible name. |
| `BR-07` | Runtime resources remain local; bundled confirmation sound still works when speech synthesis has no voices, an audio promise rejects, and only the intended setting changes storage. |
| `BR-08` | Runtime level order, skill order, level maximum, and skills-per-level derive from the exact versioned manifest. This check does not assume one skill from each strand per level. |
| `BR-09` | A new schema-3 save contains and validates the exact manifest id, version, SHA-256, and neutral placement contract. |
| `BR-10` | Browser restore accepts a valid same-manifest backup and rejects a different curriculum transactionally without changing live state. |
| `BR-11` | Manifest skills expose both selection and construction answer controls. |
| `BR-12` | Question-pick variation uses distinct prompts and sample keys under one obligation, while invariant tasks bypass a false two-card choice. |
| `BR-13` | A validator-valid current-slot fixture exercises a real two-card child pick without assuming that an arbitrary fresh session must offer one: the cards are visibly distinct, the pick/answer flow is keyboard-operable through the shipped global shortcut target rather than a synthetic native-button default action, served question ordinals persist as validator-compatible non-negative integers, and Confirm moves focus to the large feedback outcome at the actual phase transition while the same outcome persists and Next or Finish remains visible. Capturing focus before later paint probes prevents the audit itself from displacing the transition evidence. |
| `BR-14` | A same-Halifax-day reload restores active construction work and supports keyboard submission. |
| `BR-15` | An active re-teach reloads with support visible, completes as non-evidence, and clears only its re-teach queue item. |
| `BR-16` | A non-evidentiary closing question reloads, completes, records the stop reason, and closes the active session without skill evidence. |
| `BR-17` | A preview answer leaves the progress projection and all evidence unchanged; a paused regular session rejects preview selection without creating invalid mixed state. Zero-queue, cap-zero preview and regular sessions both resume their capstones after reload, while Home discards only the preview session. |
| `BR-18` | Adult-zero and exhausted-daily caps both lead to the required closing question. |
| `BR-19` | Reset requires a second action, commits schema-3 progress with a strictly later persisted placement-draft generation before attempting draft removal, clears any resumable placement UI, and leaves private pre-beta and legacy source storage untouched. |
| `BR-20` | An invalid live save boots into grown-up recovery without changing one byte; the first reset action leaves it intact and the confirmed reset recovers. Two same-origin frames prove a lifetime exclusive writer lease: the first frame saves synchronously, the second is paused on an accessible grown-up protection screen and cannot overwrite, pagehide preserves the exact terminal bytes before releasing, and a reopened frame acquires the lease. Storage denial, failed lease reacquisition, a newer same-origin save, and an oversized backup also fail closed visibly. |
| `BR-21` | Dense witnesses for every early response family, the exact MQ-004 pattern-support and MQ-031 numeral-support cases, and all three MQ-006 duration pairs keep child question, Help, and re-teach states on the first 1366×768, 820×1180, 1180×820, 1024×768, and 390×844 screen. Fourteen additive desktop/phone rows exercise the seven approved screen-native activities—pattern next, count the group, frame to number, make ten, hidden part, greatest, and least—through their exact generated visual, answer-free accessible source, real selection, and enabled Confirm. Two more rows recreate the actual MQ-118 `Find a starting point` money-budget screen at 1366×706 and 390×844, requiring its external first-cost/second-cost/remaining label key and every placement control to remain readable, visible, and reachable. Before each viewport measurement the harness requires the intended skill question, response controls, and Confirm to be rendered, so a transient blank iframe is reported as a harness-readiness error instead of a product-layout result. The check requires every response and Confirm to remain visible in natural DOM order, focuses the first and last enabled native response control, rejects document-level and question-panel horizontal overflow and any support-descendant collision with the response region, keeps essential support models visible and decorations separate, and preserves the 16 px text and 44 px target floors. The two landscape-iPad-size rows are automated geometry/focus evidence only; physical iPad Safari, touch, software-keyboard, and VoiceOver evidence remains pending until selected and executed. |
| `BR-22` | The exact approved child-string table reproduces its shipped canonicalization version and SHA-256. |
| `BR-23` | Grown-up iPad installation help is present before nickname creation and in the Grown-ups corner, states Safari’s eight honest steps, reports readiness without pretending to install, warns about separate storage, offers recovery help, traps/restores focus, and preserves the 16 px/44 px/Inter/responsive floors at 390×844. |
| `BR-24` | The install manifest preserves the reviewed app identity and icon set; independent explicit generator, worker, page, and audit oracles agree on the exact 13 shell-entry paths plus the detached manifest; the active worker reports the exact release/build/logical-cache identity and all 14 required cached URLs ready. The readiness probe starts as soon as the shipped primary frame is available and independently bounds registration readiness and the message-channel response. It messages the current controller when one exists, otherwise the ready registration's active worker, because the approved worker deliberately does not claim already-open tabs. A missing stage produces an exact stage-specific `BR-24` failure, closes its port, and allows the remaining visual/profile audit to run instead of consuming the whole-audit watchdog. The audit requires exactly one candidate physical cache whose name is that logical identity plus a 64-hex suffix, hashes the cached release manifest, requires the suffix to equal that digest, and verifies every cached response in the bound physical cache. Successful activation repeats exact-shell proof but neither calls `clients.claim()` nor deletes prior release caches; another open tab therefore keeps control and the offline bytes it may still use until its grown-up deliberately reloads or closes it. The focused page harness proves that an independent `controllerchange` never reloads at a later safe boundary, while the tab that explicitly selected Apply may reload once from the exact waiting worker's activated state. The paired worker harness proves that activation never probes or automatically navigates responsive current, suspended or unknown post-Beta-1, malformed, or true silent Beta 1 clients. A real retained-client canary must prove zero automatic navigation and then one explicit reload before that environment-dependent effect is reported as observed. A fresh worker over the verified physical cache performs zero network fetches even when the network would never settle; corrupt bytes fail closed or repair transactionally; an offline navigation loads the same manifest-bound engine after the audit server disconnects; and the three exact public legal links remain hash-verified and usable offline without intercepting other document paths. |
| `BR-25` | Child answer controls and feedback actions remain before their submit/advance controls in natural document order, without nested vertical scrollers or clipping at the audited desktop, tablet, and phone viewports. Exact MQ-121 Help and re-teach witnesses at 1366×768 additionally require the composite-area response model to remain horizontally contained while allowing the outer document to grow and scroll vertically. The exact Batch 2 witnesses require the C9 MQ-034 Help case at 768×1024 to keep all four route arrows at least 44 px, center-unobscured, outside the route grid, horizontally contained in up/left/down/right order, Inter-rendered at 16 px or larger, free of nested scrolling, and followed by a fully visible Confirm; MQ-122 ordinary play at 390×844 must horizontally contain the unit-cube SVG, visual wrapper, construction task, both fieldsets, instructional status, and every essential 44 px control while preserving outer vertical page scrolling. |
| `BR-26` | Correct and incorrect answers show exact persistent outcome text and distinct check/X symbols at both 1366×768 and 390×844; the large outcome receives focus through the single deliberate assistive-technology announcement path while Next remains fully visible and usable. The outcome is not also placed in an assertive or global live region. Incorrect outcomes must retain their visible worked review model, so hiding instruction cannot satisfy the layout gate. The desktop incorrect witness permanently protects the former 772.08 px Next bottom against the 768 px viewport. |
| `BR-27` | Automatic read-aloud and effects create and play no media while defaults are off; deliberate local Replay, Test Voice, and Test Sound remain explicit one-shot force paths without changing those defaults; non-English-only automatic fallback stays silent; cancelled callbacks cannot leak status; one re-teach Replay contains its visible instruction and prompt plus every rendered selection option’s accessible label and position in order, permits the approved additional visible operand and teaching support, and rejects malformed `[object Object]`, `undefined`, or `NaN` speech; interrupted Replay time is excluded before grading; and an active Replay cannot race a deferred, inert, keyboard-blocked transactional import. The import must advance exactly one generation beyond both live and imported records while otherwise preserving the imported projection, and a stale Replay callback cannot overwrite those exact committed bytes before reload. |
| `BR-28` | Child and Parent Test Lab rerenders preserve the initiating control’s focus, native keyboard context, and outer document scroll position while a response is being built; completed child submission intentionally moves focus to the large feedback outcome while the visible Next action remains usable. |
| `BR-29` | Phone-width pairing keeps pictorial endpoints inside non-overlapping targets and marks linked pairs consistently. |
| `BR-30` | Categorical sorting renders exact two- and three-bin colour, shape, and ordered-size cues and grades placements by stable category identity. |
| `BR-31` | Expanded Help content remains horizontally contained at every audited intermediate breakpoint, including the former 851–1180 px two-column failure range. |
| `BR-32` | Controlled-clock Home and pagehide exits persist increased elapsed and maximum-idle telemetry without changing evidence. A Halifax rollover diverts an interrupted regular question to a non-evidentiary closing question; an already-active re-teach alone may finish non-evidentially on the monotonic new day before that closing question. Both paths credit no ordinary evidence or daily practice count, log the old session on its original play day with `CALENDAR_ROLLOVER`, and allow the next regular session to start on the new play day. |
| `BR-36` | A fresh child does not see Math Quest Free Play. After exact mapped ordinary introductions, Home keeps regular Start as its first focus and exposes only eligible activity cards. Family Play Together visibly separates Maker, private handoff, and Builder activations with distinct one-person, two-person, child, and grown-up icons; Play Solo skips the handoff. Replay is exercised on each traversed view and narrates mode descriptions, all maker target choices, the current construction, and truthful feedback without disclosing the hidden family target during handoff. A deterministic dense Balance round first submits an all-ones construction and proves that visible feedback, Replay, relation ARIA, neutral strategy icons, and the absence of an incorrect-answer sound report mathematical equality while requesting a different construction; Undo-to-empty restores focus to a build control; a distinct 2-unit construction then completes successfully without early relational glyphs or duplicate live-region output. Back to games clears the transient round and restores activity focus. Ten mode/activity/maker/handoff/builder/feedback states each run at 1366×768, 820×1180, 1180×820, 1024×768, and 390×844; every row requires first-screen fit, no horizontal overflow, 64-pixel controls, 18-pixel child text, and focus reachability. The journey restores focus to the Home entry and leaves the complete stored progress bytes unchanged. Landscape-iPad dimensions remain automated browser evidence, not physical-device proof. Engine-side negative acquisition-state coverage remains in `BEH-30`. |
| `BR-33` | MQ-125 renders its generated coordinate start through the real grid controls; each arrow and Undo persist their exact route/end state; a same-day reload preserves the canonical route response in the same active session and question, permits elapsed telemetry only to advance, shows no false warning, and leaves Undo and Confirm usable; real Confirm then produces correct feedback. |
| `BR-34` | Both displayed volume methods require every layer, reveal the same usable entry control, and persist their distinct method plus exact volume; reload preserves that canonical response in the same active session and question, permits elapsed telemetry only to advance, shows no false warning, leaves the restored method/value and Confirm usable, and produces correct feedback through real Confirm. |
| `BR-35` | The optional starting-point check traverses every recommendation boundary in 10–20 questions and enumerates the exact 29 input methods reachable from the shipped initial state. Each method is loaded from an exact schema-4 draft and rendered in the real child placement wrapper under a deterministic balanced-within-one desktop, tablet-portrait, two iPad-landscape, and phone matrix; every row requires essential child text of at least 18 px, targets of at least 44 px, response-before-Confirm order, first-screen Confirm, native-control focus reachability, no nested vertical scroller, no horizontal escape, no route-grid/arrow or pattern-slot/token/Undo descendant collision, and deliberate Replay of the exact prompt. A real injected `PICTURE_CHOICE` placement notice occupies its own row before task content, and selection Replay narrates every rendered option with positional context. Start first commits a fresh private non-PII nonce/derived seed to main state without changing ordinary progress, then exposes the adaptive first question without requiring a particular family; a separately generated valid `SHARE_DEAL` witness exercises dense touch construction and incorrect feedback without constraining production order. Explicit pause/resume, crash reload, discard, correct/incorrect outcome focus, a no-response **Not sure** answer, and grown-up apply are exercised through shipped controls. **Not sure** appends exactly one minimal `{questionId,responseKind:"not-sure"}` result, preserves the distinct draft/UI kind, uses only the ordinary tap sound, focuses the neutral outcome, survives feedback reload and Next, and never enables ordinary Confirm. The result reports separate correct/incorrect/Not sure counts, exposes limited-abstention confidence and fresh retry, and labels itself an unvalidated broad heuristic. Fresh sound/speech defaults, grown-up co-play, Replay, and Not sure guidance are asserted. Corrupt, baseline-stale, and generation-stale drafts are rejected without touching progress. A controller-change signal cannot reload mid-question or at Pause. Pause must stay in the same iframe document, preserve byte-identical draft/main storage, expose the explicit Resume control, and return to the stored placement question only after that control is used. Apply exists only behind the delayed two-step `Grown-ups only` confirmation boundary, commits a strictly later draft generation, creates no ordinary evidence/log/count, and labels only earlier levels as an inferred range whose skills were not checked one by one. Dense `SHARE_DEAL` covers all six approved viewports; only the short landscape-phone profile may use natural outer flow. The iPad-size rows do not satisfy pending physical iPad evidence. |

Focused test-validity corrections in this review preserve or strengthen the
intended observable effects:

- `BR-21` now binds the exact MQ-031 support witness to the shipped
  `NUMBER_PAD` / `CONSTRUCTION` family. Its former `PICTURE_CHOICE` label was an
  obsolete harness assumption, not a child-product defect.
- `BR-25` requires MQ-069's accessible equivalent to identify the zero-to-max
  scale, unit, endpoint, and countable unlabelled tick intervals while
  rejecting any label that states the assessed mark. The former `mark N`
  predicate contradicted the answer-free accessibility contract protected by
  `QA-021`.
- `BR-26` follows the approved single-announcement contract: the persistent
  labelled/described outcome receives focus and the stable global polite live
  region remains empty. The former requirement to duplicate the same words in
  that live region was an obsolete assertion; exact outcome text, distinct
  icon, focus, review model, visible Next action, and stored attempt truth all
  remain mandatory.
- `BR-35` rebuilds its deterministic dense `SHARE_DEAL` witness against the
  fresh nonce and baseline fingerprint committed by the real Start action,
  validates that run against current main state, and requires the regenerated
  visible-task signature to equal the independent traversal witness. It then
  measures each feedback viewport in a freshly loaded document restored from
  the exact persisted incorrect-feedback draft after explicitly ending the
  outgoing document's lifetime writer lease, so that document cannot race the
  shared fixture during its real pagehide save. A fresh controller-change
  signal is then proved inert at the active question and at explicit Pause;
  a bounded load-event observation rejects any safe-boundary navigation before
  the exact stored question is deliberately resumed. Reusing the pre-Start run was
  correctly rejected by production as stale; measuring after unrelated
  lifecycle/navigation mutations could also produce an empty result. These
  harness corrections retain the lifetime writer lease plus all construction,
  persistence, font, target, focus, fit, overflow, and natural-flow predicates.
- `PROFILE-MONEY-MODEL` validates MQ-048's governed semantic fields after the
  engine's normal descriptor sanitization. Requiring stripped `data.mode` was
  obsolete; token kind and identity, the exact five candidates, grading, and
  `practice-coins-v1` remain mandatory.

The retained-client clause in `BR-24` is deliberately independent of cache
lineage and client responsiveness. The focused worker harness includes a
responsive current client, a suspended or unknown post-Beta-1 client, a
malformed client, and a true silent Beta 1 client. Activation must send no
version challenge and automatically navigate none of them, with either a
Beta-1-only or mixed obsolete-cache lineage. The trusted-HTTPS canary separately
proves that the retained Beta 1 window stays put through activation and reaches
the current verified shell only after the test performs the grown-up-equivalent
explicit reload.

`BR-25` also owns the MQ-069 installed-browser matrix. It renders length,
mass, and capacity readings in all six declared metric units at 390×844,
844×390, 768×1024, and 1366×768. It rejects nested horizontal panning,
clipped or sub-44 px answer marks, sub-16 px labels, indicator/tick
misalignment, hidden correct choices, document-width escape, and a visually
correct choice that does not produce real correct feedback. Outer vertical
document flow remains permitted only for the short landscape viewport. A
second installed-browser matrix renders all six familiar unit-choice objects
at phone and desktop widths. It requires meaningful object art, Easy-only
family cues, plain non-misleading option cards, the approved two-versus-four
choice tier contract (including the same-family sibling), absence of
pre-answer unit disclosure in the model, first-screen reachability, and real
correct feedback. The metric-reading instrument's accessible equivalent must
name its zero-to-maximum range, unit, endpoint, and countable unlabelled tick
intervals, and must not state the assessed target mark.

## Direct Playwright journey effect map

`audit/playwright/critical-journeys.spec.mjs` supplements the exhaustive
72-record browser audit with native, direct-user interactions. Each stable
case runs in installed Microsoft Edge at 1366x768 desktop and 390x844 touch
phone profiles, producing exactly 16 closed results. Playwright's ordinary
actionability checks must succeed; the suite forbids forced clicks, synthetic
event dispatch, direct DOM `.click()`, and scripted `.focus()` as substitutes
for real pointer or keyboard operation. Service workers are blocked for these
focused UI journeys so the separate PWA audit remains the update/offline
authority. Retries are zero; a retry cannot conceal a release failure. Traces
and screenshots are retained only on failure and use anonymous synthetic
state.

| Test | Protected direct-user effect |
|---|---|
| `PW-F-01` | First use can continue without a nickname, reaches Home, exposes the session start action, and opens a world through visible, enabled, unobscured native controls. |
| `PW-F-02` | Home exposes the grown-up **Check for updates** control as a visible, enabled target at least 44 px high and the control can be activated normally. |
| `PW-F-03` | A real selection response can be chosen and confirmed, producing visible correct feedback and a usable advance action. |
| `PW-F-04` | A real incorrect selection produces the distinct visible try-again outcome and keeps the correction/review path operable. |
| `PW-F-05` | Help opens through its real control while the response region and Confirm remain discoverable on the first screen at each project viewport. |
| `PW-F-06` | Native Tab and Enter operate the child flow, Home returns through its real button, and Parent Test activity remains isolated from child progress. |
| `PW-F-07` | A real `PAIR_LINK` construction is completed with native pointer/touch actions and confirmed through the shipped construction controls. |
| `PW-F-08` | An unfinished answer survives an actual page reload and returns as the same usable response rather than being lost or falsely graded. |

`audit/lib/playwright-focused-contract.mjs` rejects unknown, duplicate,
missing, skipped, retried, wrong-project, failed, or privacy-unsafe reports.
The focused contract tests also pin one worker, zero retries, installed Edge,
failure-only artifacts, the exact case/project matrix, native-action policy,
and disposable-server cleanup. This automation is rendered-browser evidence,
not physical iPhone/iPad, Safari, VoiceOver, software-keyboard, pronunciation,
or child-comprehension evidence.

## Playwright Deep UX Census effect map

The census is a separate alternating-beta diagnostic owned by
`audit/lib/playwright-deep-ux-census.mjs`. It is required for Beta 4, 6, 8,
and later even-numbered semantic-version betas. It never runs completely in
ordinary development and does not enlarge or repeat the single frozen-
candidate gauntlet. The local 100-cell mode is always non-certifying. Beta 6
is even-numbered, so its full census is mandatory on the exact frozen
candidate and must pass before publication.

| Contract | Protected effect |
|---|---|
| Deterministic source inventory | Exactly 126 skills × 2 tiers × 3 representations × 3 worlds × 32 ordinals produce 72,576 source questions under one exact seed and engine/curriculum identity. Missing or extra source combinations fail plan validation. |
| Risk-selected plan | Maximum-risk structural signatures plus required skill/tier, method/representation/theme, and model/representation witnesses form a closed, hash-bound scenario and six-viewport cell set. The planner cannot silently hand-pick only favourable examples. |
| Rendered state census | Every selected cell checks initial and changed-response states, visibly proved expected-answer disclosure, and a visibly proved teaching model when one exists. It prefers an enabled, unpressed native response control so an already-selected default cannot create a false no-effect result, then waits for the production render-settled signal before inspecting or continuing. It uses native click/tap/form actions with normal Playwright actionability and `scroll: "none"` where first-screen discoverability matters. Later-grade content on the 844 by 390 short-landscape profile may move only `document.scrollingElement`, prove that it is the outer document element and actually moved, then repeat the same real-control/no-auto-scroll actionability proof. Early levels and all other profiles remain first-screen strict. Unapproved nested vertical scrolling and ancestor-clipped controls fail; only the governed large route-grid scroller is exempt. |
| UX and accessibility invariants | The rendered question enforces 16 px text, 44 px controls, accessible names, visible response/grade regions, no horizontal clipping or placeholder text, valid question contracts, unchanged child-save bytes, and zero unexpected requests, page errors, or console errors. |
| Anomaly-only evidence | Passing cells create no screenshots, traces, or videos. A failure produces synthetic WebP, AI-mode ARIA-with-boxes, and geometry records plus one compact closed report. The workflow explicitly includes these hidden `.tmp` paths in its failure upload so retained evidence cannot silently disappear. |
| Cadence and identity | Complete mode requires the exact frozen protected-main candidate on GitHub-hosted Windows, reviewed Edge 1.62.1 toolchain identity, exact server-root and served-payload digests, all six project shards, no retries/skips/unknowns/duplicates, and a matching executed-cell digest. |
| Claim boundary | The census does not replace the 72-result browser audit, mathematics or grading oracles, human play testing, physical devices, Safari/iOS, assistive-technology speech, pronunciation, or installed-PWA evidence. |

`audit/tests/playwright-deep-ux-census.test.mjs` effect-tests cadence mutants,
the exact 72,576 inventory, balanced 100-cell non-certifying mode, closed plan
and report schemas, missing/duplicate/identity/privacy mutants, native-action
rules, context-level network observation, and anomaly-only artifact policy.

The Beta 6 frozen-candidate census classified its 27
`PRIMARY_RESPONSE_REQUIRES_SCROLL` records before correction. Nine were product
layout defects: MQ-028, MQ-029, MQ-034, and MQ-040 on the short-landscape phone,
plus MQ-076 on the portrait phone. Their exact Parent Lab compositions were
compacted without lowering 16 px text or 44 px control floors and without adding
nested scrolling. The other 18 records—later-grade MQ-053, MQ-071, MQ-089, and
MQ-125 on the short-landscape phone—were harness false negatives: Chromium
mobile emulation ignored Playwright mouse-wheel and synthesized CDP scroll input.
The corrected fallback is limited to that already-approved later-grade profile,
moves only the outer document element, and still requires a real Playwright
tap/click with automatic scrolling disabled. A focused five-cell rendered Edge
check protects the product correction; the next exact frozen-candidate census
must protect the complete matrix before publication.

## Browser-runner completion and capacity effect

`audit/tests/pwa-release.test.mjs` constructs the real installed-browser
argument vector from the exported browser-audit timing policy. It requires an
isolated profile, exact loopback-only resolver and debugging bindings, a random
debugging port, unchanged anti-background flags, and the exact audit URL last.
It rejects both `--virtual-time-budget` and `--dump-dom`. The executed
completion-polling regression observes false, false, then true before capture
and separately proves that a never-completing page rejects at its exact
deadline. This prevents initial-load serialization from masquerading as a
complete audit.

The same test requires the exact owner-approved doubled limits: a 38-minute
real-time in-page watchdog, a separate 40-minute Node wall limit, and the
45-minute hosted job with at least five minutes for setup, reporting, and
evidence upload. It also reserves at least two minutes between watchdog and
wall termination for CDP serialization and clean browser shutdown. It executes the page
watchdog and requires a fail-closed BR-00 payload containing the current
progress marker, active frame, and completed-result count. Per-scenario
readiness limits remain unchanged. The runner accepts evidence only after the
page's synchronous `data-audit-complete` fence, treats an initial transitional
document with no root element as incomplete rather than an exception, captures
the completed DOM over CDP, and validates a closed payload schema against the explicit 72-result ID
oracle. It rejects missing, duplicate, unknown, extra-field,
unsupported-status, or fabricated-aggregate payloads and recomputes failure
totals from the trusted records. The same absolute browser deadline bounds the
`Browser.close` request, graceful-exit allowance, and forced process-tree
termination. A genuine CDP/control failure bounds termination and exit waits to
the reviewed ten-second cleanup grace instead of silently consuming the
remaining browser wall deadline, so a harness fault is reported promptly.
Timeout, early exit, malformed payload, nonzero exit, forced termination, or
profile-cleanup failure remains non-passing.

This protects hosted run `30505640570`, where Edge exited normally inside
`BR-21`, and run `30515361457`, which exited normally in the late MQ-121
visual-regression witness, both before the final payload existed. Run
`30516325797` produced a 17.5-minute BR-00 record after 22 successful groups
without a scenario error. A larger allowance then disproved simple capacity
exhaustion: run `30517131201` returned a complete error payload without
exhausting the global browser budget, preserved state while missing only
post-reload UI in BR-04/BR-17, and left a placement frame inert before its Web
Lock callback. Those cross-process
scheduling symptoms are why accelerated virtual time is now prohibited. The
replacement real-time controller completed the local installed-Edge matrix
72/72 on 2026-07-30; two consecutive hosted 72-result passes on one exact
commit remain mandatory evidence.
The same module effect-tests disposal of every ordinary scenario frame:
`pagehide` must synchronously reach the game before physical iframe removal,
so its exclusive progress-writer lease cannot survive nondeterministically
into the next scenario. It also executes the anonymous first-use Home waiter
across a simulated safe-boundary document transition instead of accepting a
single event-loop sample. These checks protect hosted run `30513379330`, which
preserved the correct anonymous record and progress but sampled Home during a
reload, then later stalled while entering a re-teach scenario.
Every scenario is now paintable before its first readiness wait, and the
installed-browser argument vector disables Chromium's background timer,
occluded-window, and renderer throttles for the synthetic multi-iframe audit.
Executable style, call-order, and launch-argument checks protect hosted run
`30514438006`, which otherwise left a complete hidden placement document inert
and empty while its progress-lock bootstrap callback was background-starved.

## Page-adapter effect map

`audit/tests/page-adapter-effects.test.mjs` extracts and executes the shipped
adapter function bodies in a restricted Node VM. Its fake service-worker,
sharing, download, URL, and timer effects are counted; source-text matching
alone cannot make these checks pass. During development,
`audit/run-audit.ps1 -DevelopmentOnly` runs the exact PWA manifest check and
the complete PWA/page-adapter effect module with its staged Node 24 runtime.
After the public candidate is finalized and frozen, `audit.bat` reruns them
inside the single complete pre-publication certification cycle and fails closed
on either class of defect. The module contains exactly 27
effect-sensitive tests.

| Test | Protected adapter effect |
|---|---|
| Beta 1 migration, retained-curriculum cutover, and empty-store cutover | After the protected writer lease is held, the adapter writes an exact constant non-PII durable guard. A compatible migration re-reads the exact legacy source before copying and checks it again across the protected write boundary. The immutable curriculum-incompatible Beta 1 envelope is instead recognized only by its closed schema/product/manifest/digest/key identity plus successful full-state validation through the existing engine validator: its bytes remain untouched while `beta1-retained-to-protected-v1` encloses an exact fresh Beta 6 write and `beta1-retained-current-curriculum-v1` becomes terminal only after source, protected bytes, and marker re-verification. Identity-only fixtures and malformed nested values are rejected. The terminal marker prevents virgin-progress rediscovery, while a missing protected save, malformed or unrecognized legacy envelope, source/guard race, failed terminal write, and delayed source/guard storage event all fail closed and roll back only this tab's byte-matching protected write. When both stores were initially empty, an `empty-to-protected-v1` guard is written before the first protected save and both the source and exact guard are checked before and after that write. Extracted late-source and late-guard races with delayed events produce zero protected writes. Independent no-event races injected at blank-save entry, immediately after the blank write, and during marker removal are detected by the enclosing and closing checks; a byte-matching partial blank is removed, while a forced removal failure leaves the guard. A closing source race re-establishes the empty marker before rollback. If marker removal is followed by simultaneous Beta 1 arrival and total local-storage read failure, the current launch stays blocked even though neither rollback nor re-guarding can complete. On the next readable launch, the adapter independently validates the protected bytes and classifies them as virgin only when their canonical export exactly equals `createInitialState(saved.maxSeenPlayDay)`. A compatible Beta 1 record therefore outranks that virgin record even with no external marker; settings, placement generation, session, malformed, migrated, or otherwise non-virgin protected states never enter this fallback. A changed guard without its source remains fail-closed. Failed guard cleanup remains fail-closed, and source/guard storage events independently stop every active cutover. |
| Safe-boundary and explicit Home/Retry checks | Automatic update discovery runs from nickname choice, Home, and the Grown-ups corner but not during a question. Home visibly exposes a grown-up `Check for updates` control with live checking, current, failure, and verified-update states; a ready update exposes its safe-boundary Apply action there. The actual `pwa-check` and recovery-dialog `pwa-retry` paths invoke readiness and `registration.update()` even inside the ordinary 60-second debounce window, while an uninitialized Home check starts the reviewed offline/update lifecycle. |
| Web Share success and cancellation | A successful file share creates no download URL. A grown-up's `AbortError` cancellation also creates no download, URL, or delayed effect and reports cancellation honestly. |
| Web Share rejection fallback | A non-cancellation share failure clicks one local download, then removes its temporary anchor and revokes the exact object URL on the adapter's delayed cleanup path. |
| Activation timeout and retry | A proved waiting-worker activation timeout clears applying state, reports that the current version remains usable, exposes recovery, and accepts a second explicit Apply attempt with a fresh challenge, listener, timer, committed save, and challenged skip-waiting message. A duplicate Apply while any proof/save/activation step is pending has no effect. |
| Active/candidate lifecycle separation | Candidate discovery, caching, redundancy, and update-check failure effects leave a ready active-shell phase and its readiness detail unchanged. First control acquisition performs no immediate save or reload, preserves unfinished first-use input, and marks the exact-cache reload pending; initial acquisition or a later replacement during a question then saves and reloads exactly once only after Home or the Grown-ups corner is reached. |
| Waiting-worker activation challenge | The page generates exactly 256 random bits, sends them to the exact `registration.waiting` object, and accepts only an exact release/build/cache/path conjunction, the `waiting` protocol label, fresh timestamp, and challenge echo. That label is not an independent worker-side observation of its registration slot. A missing, malformed, mismatched, late, or non-ready page proof, or a changed waiting object, produces zero progress commits and zero activation messages. No handshake, an invalid or mismatched challenge, a consumed/stale challenge, or any cache mutation after readiness produces zero `skipWaiting()` effects. One fresh matching challenge plus a second full exact-readiness pass produces exactly one. |
| Browser-smoke legal MIME | The real loopback audit server returns exact legal-document bytes and normalized `text/markdown` response types, so service-worker MIME validation is exercised against HTTP effects rather than only an in-memory response mock. |
| Fresh versus verified-active PWA copy | Checking, caching, error, and activation-failure states promise preservation only while a current controller plus validated active readiness proves an exact shell. The same states on a fresh, uncontrolled, or unready page make no offline-availability claim and direct the grown-up to stay online or retry. |
| Caregiver-safe PWA errors | Hostile registration and update exceptions containing script URLs, scope tuples, quotes, and doubled punctuation are effect-injected into shipped lifecycle functions. The resulting PWA state contains only bounded fixed caregiver copy and an optional allowlisted diagnostic identifier; no exception text is rendered. |
| Transactional exact-shell population | The worker derives content-addressed physical live/staging cache names from the exact embedded manifest hash, so two scripts with the same logical identity cannot collide. It fills and revalidates staging before touching its candidate cache. An exact live cache receives zero write effects and survives same-length bad network bytes, wrong MIME, redirect, unsuccessful status, or a cross-origin response. Injected mid-copy failure removes an already-invalid partial candidate, while readiness fails closed; a later exact repair restores all 14 URLs. Two simultaneous repair messages share one population/network effect and cannot race staging. Activation repeats the full exact proof, removes only candidate staging, and neither claims clients nor deletes any prior release cache, so post-handshake mutation and older open tabs preserve their fallback bytes. |
| Non-mutating manifest preparation | The generator writes a current exact manifest and bound worker copy only into a caller-provided empty review directory, refuses overwrite, and leaves the repository's frozen manifest and worker bytes unchanged. It carries the prior content-addressed physical live/staging caches into the exact obsolete list while excluding both current caches. The release freeze is a separate explicit `--write` operation with rollback. |

## Placement-adapter effect map

`audit/tests/placement-adapter-effects.test.mjs` extracts the shipped
starting-point adapter functions and lifecycle listeners into restricted Node
VMs. It counts main-save, draft-save, purge, focus, sound, announcement, and
render effects so placement isolation and transaction ordering cannot pass
through source-text matching alone. The complete audit runs this module as a
mandatory focused gate; `BR-35` and `VIS-PLACEMENT-LAYOUT` retain the
installed-browser and rendered counterparts. The module contains exactly 20
effect-sensitive tests.

| Test | Protected placement-adapter effect |
|---|---|
| Completed feedback restore | A crash after the final explicit answer restores that exact question, correctness outcome, and recommendation in feedback phase before showing the result; it neither deletes the recoverable draft nor changes main progress. |
| Stale and recursively corrupt draft | An invalid nested response shape, progress-baseline mismatch, or persisted-generation mismatch reaches no child renderer, removes the unusable draft when storage permits, clears placement UI state, reports a bounded grown-up notice, and leaves main state unchanged. |
| Prior-contract unfinished draft rejection | An unfinished `starting-point-v2` draft is rejected and removed before any placement screen renders, reports bounded recovery guidance, performs zero ordinary-progress writes, and leaves canonical main progress unchanged. |
| Failed next-question persistence | If the next draft cannot be committed, the adapter rolls back to the already durable prior feedback question and correctness outcome rather than displaying unpersisted work. |
| Correct structured construction grading | A real `PLACE_VALUE_BUILD` placement response is serialized into the public engine payload, graded correct, appended exactly once, committed only to the recoverable placement draft, and shown through exactly one focused-feedback announcement path while the global live region remains unused and ordinary progress bytes remain unchanged. |
| Monotone answer prefix across lifecycle boundaries | Correct construction and selection answers remain one exact monotone prefix through feedback, pause, same-tab resume, feedback reload, Next, question pause, and question reload. Every restored question matches the engine cursor, preserves its selected response where applicable, and produces zero ordinary-progress writes. |
| Universal **Not sure** | The shipped no-response action appends exactly one minimal non-correct result, uses no fabricated answer or ordinary grader call, persists only the draft, presents/focuses the distinct neutral `Not sure. Let’s try another.` state and icon without playing the punitive incorrect-answer sound, and leaves main state unchanged. Its feedback kind survives a crash independently of the minimal engine answer. |
| Placement speech and feedback replay | Start, Next, and deliberate Replay all route through the evidence-rich question speech builder. Selection speech names every currently visible option in display order, while feedback Replay exactly matches the visible correct, incorrect, or neutral outcome. |
| Fresh transactional start and retry | Start prepares a cloned engine state and run, commits the incremented private nonce in main progress before exposing any schema-4 draft, then renders. Injected main-commit failure leaves live state, placement UI, and draft storage unchanged. Retry discards only the completed draft and repeats the same ordering with a strictly newer nonce and derived seed. |
| Grown-up application boundary | The result button opens an accessible `Grown-ups only` alert dialog and makes zero progress commits. The opening gesture and an early repeat cannot commit; only a delayed second explicit confirmation passes the chosen level to the transactional apply path. |
| Draft-only response and lifecycle writes | Response mutations, hidden-page snapshots, and pagehide during placement call only the draft writer; ordinary progress receives zero writes, while the lifetime writer lease is still released on pagehide. |
| Failed recommendation commit | If the grown-up's recommended-level progress commit fails, original main state and result UI survive and the recoverable draft is not deleted. |
| Post-commit conflict purge | After a successful main-state replacement, the old draft is necessarily stale; the dedicated purge removes it even if an earlier draft compare-and-set conflict or read warning existed, then clears all draft-conflict bookkeeping. |
| Reset/import transaction ordering | Reset and valid import build a replacement with a strictly later persisted generation, purge a paused draft only after those replacement progress bytes commit, then clear placement UI. |
| Surviving-draft generation invalidation | The adapter safely parses a bounded generation floor from draft bytes independently of main-save readability. Extracted recovery reset, recovery import, and placement-apply paths begin from a generation-zero fallback beside a real surviving generation-one draft, commit generation two, inject a throwing `localStorage.removeItem`, and prove a simulated reload rejects the physical old draft before child rendering. Malformed, oversized, negative, or unsafe floors conservatively contribute zero. |
| Failed replacement write preservation | Effect-injected reset and import commits fail before replacement; live state keeps its exact prior generation, placement UI remains recoverable, and draft purge receives zero calls. Failed apply has the same separately measured transaction boundary. |
| Placement construction keyboard path | Construction keyboard input routes through the placement response checkpoint and draft persistence path, marks the model touched, and never falls into ordinary-session persistence. |
| Mobile minimum geometry | The shipped phone and tablet placement minimum-height formulas fit their declared portrait viewports before content-specific rendering; browser evidence separately proves real controls, outcome focus, text/target floors, and allowed outer-flow behavior. |

`BEH-12` formerly required imported state to be byte-identical to the backup.
That assertion became obsolete when import replacement acquired the approved
draft-generation invalidation effect. It now requires equality after projecting
only that generation back to its prior value and independently requires an
exact `+1` generation transition. `BEH-27` additionally proves
baseline-identical reset/import staleness, advancement beyond either local or
imported generations, and transactional preservation on rejected imports.

## Private QA Tour effect map

`audit/tests/qa-tour.test.mjs` extracts the shipped private-tour manifest,
adapter functions, and action listeners into restricted Node VMs. Its exactly
12 effect-sensitive tests protect the fixed 50-question review contract,
progress isolation, fail-closed snapshot and report behavior, and local-only
report delivery. The final release audit runs this module in the mandatory
holistic regression group; representative rendered browser evidence remains
required for desktop and phone layouts.

| Test | Protected private-tour effect |
|---|---|
| Exact hidden URL and adult stop | Only the exact versioned query requests the tour. Cold bootstrap routes first to the explicit grown-up confirmation screen, without starting the tour, restoring a placement draft, or resuming a child session. |
| Grown-up confirmation content | The confirmation screen identifies the 50-question adult review, states that child progress does not change, exposes exact Start and Cancel actions, and renders neither child-name nor progress data. |
| Explicit confirmation route | Rendering `qaConfirm` calls only the dedicated confirmation view rather than falling through to a child, session, lab, or grown-up screen. |
| Frozen representative fixture | The immutable 50-item fixture contains 50 unique specifications and generated question/sample identities, spans all 21 levels, six strands, and 26 generator profiles, explicitly includes all six governed `STRATEGY_BUILD` skills, requires the runtime's exact 35 renderer-capable methods, and requires the fixture's generated methods to equal the independent exact 33-method release-reachable oracle. `NUMBER_BOND` and `NUMBER_CHOICE` are explicit renderer-only exclusions rather than circular omissions. |
| Exact action dispatch | Start, Cancel, Review and continue, Previous, Submit, and Exit gestures dispatch only their named private-tour actions in the appropriate confirmation or active-tour state. |
| Disposable finding flag | Toggling the issue flag mutates only the current in-memory QA record and no child or ordinary progress state. |
| Revocable local visual snapshot | The rendered adult-only QA question is rasterized through a temporary local Blob URL, that exact URL is revoked after image load or failure, and capture fails closed without an embedded data-URI asset or network effect. |
| Awaited finding snapshot | A note or flag becomes reviewed and advances only after its local visual snapshot succeeds and the exact deterministic question identity is recorded. |
| Snapshot failure containment | A failed visual capture keeps the finding unreviewed, preserves the current question, prevents advancement, and exposes a retryable integrity error. |
| Fail-closed report construction | An unreviewed item, a noted or flagged item without its snapshot, a fixture-integrity error, or changed protected storage creates no report blob, share, or download and instead exposes an actionable failure. |
| Closed privacy allowlist | The exact report schema contains only reproduction-safe environment categories, deterministic question data, findings, and required snapshots; it records no child name/progress, raw user agent, origin, IP address, or automatic network effect. |
| Local iPad share and download fallback | Supported file sharing produces one local JSON file and no download; unsupported sharing produces one local blob download whose temporary URL is revoked, with zero network effects on both paths. |

## Holistic defect-regression map

During development, `audit/run-audit.ps1 -DevelopmentOnly` runs
`audit/tests/holistic-functional-regressions.test.mjs`,
`audit/tests/holistic-child-ux-regressions.test.mjs`,
`audit/tests/qa-tour.test.mjs`, and the shipped-adapter syntax test as focused
gates. Once the public candidate is frozen, the
release-only `audit.bat` runs them again within the single complete
pre-publication cycle. These focused checks supplement, but do not replace, the
rendered browser, hosted, and physical-device evidence required by the release
documents.

`audit/tests/certification-cadence.test.mjs` independently validates the closed
machine-readable cadence record and its three public inventories, prevents
ordinary CI and the local on-change
watcher from invoking complete certification, requires the release workflow to
bind the exact candidate SHA without `--technical-only`, and requires Pages to
consume successful exact-commit certification without repeating coverage,
mutation, exhaustive-generator, or engine gauntlet stages. It also protects the
separate hosted-Windows identity observer: dispatch is manual and main-bound,
the selected candidate must equal the workflow SHA, browser and runner fields
must form one canonical tuple, the artifact must say explicitly that it is not
certification, no complete-audit entry point may appear in the workflow, and
Pages may continue accepting success only from `audit.yml`. Mutation checks
reject a changed candidate, certification-like status, unsupported browser,
short version or hash, pending runner identity, extra key, or noncanonical line
ending.

| Defect / check | Protected observable effect |
|---|---|
| `QA-002` / `QA-003` | Every generated Grid Route begins at the coordinates shown to the child, accepts only a valid prefix of the displayed route, derives its endpoint from those moves, grades only the complete exact route, and survives canonical active-session persistence. A failed response save restores the prior visible response and exported state instead of leaking rejected work into a backup. |
| `QA-004` | Every displayed rectangular-prism strategy (`count` and `multiply`) reaches an exact answer-entry state and grades the same mathematically correct volume. |
| `QA-005` | A destructive confirmation cannot run during the opening pointer gesture; a later deliberate confirmation is the first activation allowed to mutate progress. The mandatory browser review additionally covers the real modal, all destructive targets, cancellation, keyboard containment, focus return, and rapid mouse/touch activation. |
| `QA-006` / `QA-016` | Cold boot creates no child UI before the exclusive progress lease settles. After a successful lease, fresh Home focuses Start and a resumed choice focuses its first task card before PWA work begins. Browser races separately prove clean-warning, inert failure, response preservation, and exact Tab/Shift-Tab order. |
| `QA-007` | Universal early physical guidance names only grown-up-approved large materials. MQ-048 has one frozen public five-row token/value authority from which the renderer allowlist and first-use guide derive. Every Canadian denomination maps to a distinct original practice token; the ordinary stimulus stays answer-free; unknown designs are rejected; and the independent exact oracle cannot collapse the five designs into one semantic prompt row. A child session with no real MQ-048 evidence—including placement-inferred progress—first renders all five accessible mappings with no answer controls. Ready opens the unchanged ordinary question without submitting an attempt; preview, re-teach, capstone, and later evidenced sessions do not trigger the guide. The physical-phase checkpoint survives exact export/load. |
| `QA-008` | Direct construction emits the primary-control class whose winning foreground token is white; computed rendered contrast remains a browser assertion. |
| `QA-009` | The original zero number-connection variant renders a bounded, explicitly labelled empty set, and its teaching support visibly connects that empty set to numeral `0`; the added frame-to-number variant is covered separately by the exact screen-native semantic and rendered-browser matrices. |
| `QA-010` | Deliberate Replay at the two-card choice point speaks positional cues and both currently displayed candidate prompts in order while automatic speech remains off by default. |
| `QA-011` | One-to-one teaching support preserves the semantic object kinds and renders visible matched relationships plus any unmatched object. |
| `QA-012` / `QA-013` | Fresh-install/update failures never claim an unproved offline version and never expose raw worker exceptions; the PWA/page-adapter suite injects and inspects the resulting state and copy. |
| `QA-014` | Runtime administrative notices are structurally excluded from child question, fatigue, capstone, and completion screens. |
| `QA-015` | The time-cap parser accepts only blank or canonical whole minutes 1–14. State validation rejects zero and out-of-range caps; legacy zero migrates to the ordinary stage cap and legacy overflow clamps to 14 minutes without ever creating a zero-millisecond session. |
| `QA-017` | A full 24-character nickname survives greeting generation and uses the wrap-safe Home layout; rendered Latin, CJK, and emoji geometry remains a browser assertion. |
| `QA-018` | Temperature, elevation, and score questions remain within their context-specific plausible bounds and preserve their displayed units. |
| `QA-019` | Each world generates only its approved early one-to-one object vocabulary, including the stop-to-capstone path. |
| `QA-020` | The pristine name-gate live alert is hidden and occupies no intended warning state until validation produces a real error. |
| `QA-021` | Metric-reading construction uses unit-correct length, mass, and capacity instruments; the depicted endpoint, pointer, or liquid line shares the exact coordinate of its target tick while the accessible equivalent exposes countable unlabelled intervals without naming the assessed mark; all contiguous `0..maximum` answer marks remain unique 44 px controls with 18 px text in a wrapped, non-scrolling choice grid; Easy labels more scale landmarks than Hard/Target; adjacent marks grade incorrectly; and the zero-origin prompt cannot invite counting the zero tick as an extra unit. |
| `QA-022` | Every metric-unit object maps to the correct measurement family and situation, appears as a meaningful first-party picture, and has a prompt that explicitly names the intended measurable attribute so another property of the same object cannot become a defensible answer. The exact unit stays out of the unanswered item/data descriptor while teaching support may reveal it. Seed-shifted paired choice/read cycles cover all six units. Easy uses a family cue and two evidentiary choices; Hard/Target removes the cue, uses four choices including the same-family sibling, and reads scales through 20 rather than Easy’s 10. |
| `QA-023` | The rendered journey map and grown-up progress table retain the neutral `Inferred range` disclosure, while the starting-point entry/result explicitly say `unvalidated broad` and `Heuristic starting range`. Co-play, fresh sound/speech defaults, Replay, and Not sure guidance are present. The result renders exact correct/incorrect/Not sure counts, limited confidence, conservative handling, and fresh retry, while no surface describes inferred skills as covered, tested, completed, or mastered. |
| `QA-024` | Home does not expose a misleading Home control that can only rerender the current screen. Session, grown-up, and parent-information headers retain their working route back to Home. |
| `QA-025` | A new regular session rotates across the level's due obligations instead of repeating one skill, while each skill's early exposures preserve declared Concrete-to-Pictorial progression. |
| `QA-026` | Pictorial-only or reversed-phase evidence cannot create mastery. Ordered Concrete-to-Pictorial evidence can, and obsolete Beta 2 P-only mastery records migrate to a safe non-solid state instead of retaining false mastery. |
| `QA-027` / `QA-027B` | Pre-response speech, Replay, semantic model labels, and structured clock/angle/metric control labels expose an answer-free equivalent stimulus rather than stating the assessed result. Object quantities are individually countable, clock hands are described by position, and unlabeled scale/angle intervals are grouped without naming the final measurement. Help, re-teach, and feedback instead use a cloned worked descriptor, resolve number-bond unknowns, preserve proportional-bar values with units, name categorical sort bins, reject malformed object/undefined/NaN speech, and expose the worked result in visible and accessible output. Concrete physical-task instructions still name the current mathematical task. This replaces the obsolete assertion that required exact target narration before an answer. |
| `QA-028` | MQ-111 estimates division from the displayed rounded operands exactly, including fractional quotients, under an independent oracle; MQ-035 uses neutral grammatical attribute-comparison wording. |
| `QA-029` | Every child answer submission renders and focuses an explicit, large `Correct.` or `Not correct yet.` outcome before progression is offered; its labelled focus group is not simultaneously an assertive status/live region. |
| `QA-030` | Early pattern tasks render every answer token as a visible glyph with a spoken label, so blank or visually identical cards cannot be presented to a pre-reader. |
| `QA-031` | Help controls remain enabled and exposed only for an active question or re-teach phase; pick, physical, feedback, capstone, missing-question, and non-session states hide and disable every Help control. |
| `QA-032` | Each feedback transition clears stale global live text and focuses the one labelled outcome exactly once with no second live-region announcement. |
| `QA-033` | The browser placement oracle completes every adaptive level-boundary traversal with only valid child responses. Comparison pair-link fixtures include the visible `more`/`fewer`/`same` decision; strategy-bearing classification constructions preserve their textual result; and multiplication/division fact-family fixtures use the four governed multiplication/division equations. This replaces stale fixture assumptions from before those task contracts changed and prevents an obsolete oracle from aborting all later rendered-browser checks. |
| `QA-034` | A multiplication/division fact-family question renders the exact four governed multiplication and division equations plus only same-family distractors, labels the source as product and factors, and the real engine grades the rendered four-equation response as correct. This permanently detects the defect where MQ-063 displayed addition/subtraction equations that its grader could never accept. |
| `QA-035` | Across both tiers and ordinary/re-teach generation, MQ-031 remains a valid `NUMBER_PAD` construction and grades its exact numeral correctly. The browser supplement is bound to that generated family, preventing an obsolete `PICTURE_CHOICE` label from falsely failing every otherwise valid layout row. |
| `QA-036` | Synthetic browser scenarios hand the lifetime progress-writer lease across an explicit barrier and then require two task-separated observations with no held or pending writer before the next frame starts. Ordinary scenarios reject progress-protection and save-recovery screens with exact lock diagnostics; only BR-20's deliberate fail-closed fixtures may opt in. The placement lifecycle independently rebuilds its dense `SHARE_DEAL` witness on the committed nonce, and feedback uses one focused outcome without a duplicate live-region announcement. BR-21 retains its fixed seven-second readiness bound, all 375 base layout rows, 14 screen-native activity rows, two placement money-label rows, and every existing geometry, focus, font, and control predicate. |
| `QA-037` | Exact short-viewport packing preserves the established child text and target floors. The proportional-bar renderer moves labels outside any narrow segment into a readable keyed legend, while the BR-21 supplement proves that behavior on the actual `Find a starting point` route rather than a substitute practice screen. |
| `QA-038` | Every `STRATEGY_BUILD` response begins with its answer-free source model before the method controls. This protects the number-bond meaning of MQ-040 and the visible parity expression of MQ-097; the rendered-browser model and viewport matrices independently require the real derived model and first-screen controls. |
| `node:test` screen-native activity matrix | Across deterministic ordinals and both tiers, the seven approved activity families retain their exact prompt/model pairing, unique correct answer, answer-free visual source, truthful accessible description, and real grading effect. Pattern, object-group, frame, covered-frame, and number-card sources remain visibly distinct rather than collapsing to text-only worksheets. MQ-019 additionally exercises answers 0 and 10 and requires every frame/number question to expose four distinct candidate numerals inside 0–10, preventing boundary deduplication from collapsing the answer-position sample. |
| Fresh defaults | Every newly created state keeps automatic speech and bundled sound effects off until a grown-up deliberately enables them. |

### Beta 3 and Beta 4 affected-check validity classification

- **Product defects corrected:** one-obligation session queues, incomplete CPA
  mastery, inaccessible visual operands, generic physical instructions,
  double-rounded MQ-111 estimates, and ambiguous MQ-035 wording.
- **Obsolete test assumptions corrected:** fixtures that treated
  Pictorial-only evidence as sufficient mastery or required a repeated
  single-skill queue were test defects. They were replaced with stronger
  ordered-phase, multi-skill, migration, and persistence assertions. The
  visual-scenario constructor now holds a skill at its final declared phase
  instead of cycling back to Concrete, and the task-type mutant witness covers
  every CPA phase so only the mutated task-type rule can change its outcome.
  The speech audit now requires Replay to contain the complete visible prompt
  while permitting the approved task and model context that makes the audio
  more comprehensible; exact bare-prompt equality was obsolete.
- **Harness defects corrected:** `Browser.close` could exceed the intended
  wall-clock budget, and a count-shaped browser payload could be trusted
  without an exact closed result oracle. Bounded-close and payload-forgery
  regressions now protect both effects. The exhaustive `PAIR_LINK` fixture now
  records the required visible `more` / `fewer` / `same` relation before asking
  the real engine to complete, serialize, and grade each comparison; omitting
  that child decision can no longer misreport every MQ-003 question as a
  generator failure. Its `STRATEGY_BUILD` fixture uses the independent
  six-skill oracle to construct governed method, intermediate work, and result
  bytes before self-grading, rather than trusting hidden question answers. The
  same exhaustive oracle recognizes the approved five-token MQ-048 selection
  boundary, allocates one bias bucket per rendered option, and builds MQ-063
  multiplication/division facts instead of an obsolete addition family.
- **Environment or approval gates:** no external, hosted, privacy, or
  physical-device result is converted into a pass by these corrections. Those
  gates remain pending until the exact frozen Beta 6 candidate supplies the
  evidence required by the release documents.

## Manifest-driven visual and profile map

The visual suite selects its cases from the canonical manifest, not from
historical curriculum ids. Each `PROFILE-*` check requires deterministic,
self-gradable questions; declared task-type coverage; valid input contracts;
truthful semantic model descriptors; and preserved task type on submission.

| Test | Protected effect |
|---|---|
| `VIS-MANIFEST` | The visual audit is bound to the exact manifest, every runtime skill uses a declared profile, and generation/mastery/runtime task-type contracts agree. |
| `VIS-PLACEMENT-LAYOUT` | Every one of the exact 29 input methods reachable through adaptive starting-point traversal renders in the real placement wrapper at desktop, tablet portrait, 1180×820 iPad landscape, 1024×768 iPad landscape, and phone dimensions—145 method/viewport rows rather than a distributed sample. Every method preserves an 18 px essential child-text floor, 44 px target floor, answer-before-Confirm order, first-screen Confirm, native-control focus reachability, exact input-method identity, prompt Replay, selection option/position narration, freedom from horizontal escape or nested vertical scrolling, and freedom from route-grid/arrow and pattern-slot/token/Undo descendant collisions. The `PICTURE_CHOICE` snapshot injects a real placement notice before every child task region. Dense `SHARE_DEAL` additionally exercises all five rows plus 844×390; only the short landscape-phone profile may use natural outer flow. A shipped **Not sure** action focuses the neutral outcome. Creating and discarding fixtures writes only the separate schema-4 placement draft and leaves main progress bytes identical. Automated iPad-size rows do not satisfy pending physical iPad evidence. |
| `VIS-PWA-STATUS` | The install dialog renders active offline readiness, candidate-update status, and candidate-update errors in separate diagnostic nodes; candidate wording cannot replace the active readiness label. |
| `VIS-CONSTRAINTS` | Every sampled generated question obeys its quantitative and structural manifest constraints; clock-reading prompts do not print the answer, tenths/hundredths models preserve their place-value fraction, and coordinate candidates remain in the declared bounds and quadrant. |
| `PROFILE-QUANTITY-IDENTIFY` | Quantity-identification prompts and visual models satisfy the common profile contract. |
| `PROFILE-QUANTITY-ORDER` | Quantity-order prompts and number-line or visual models satisfy the common profile contract. |
| `PROFILE-QUANTITY-COMPARE` | Quantity-comparison prompts and comparison models satisfy the common profile contract. |
| `PROFILE-PLACEVALUE-COMPOSE` | Place-value composition prompts and models satisfy the common profile contract. |
| `PROFILE-PATTERN-EXTEND` | Pattern-extension prompts and models satisfy the common profile contract. |
| `PROFILE-PATTERN-RULE` | Pattern-rule prompts and models satisfy the common profile contract. |
| `PROFILE-OPERATION-MODEL` | Operation-model prompts and concrete/pictorial descriptors satisfy the common profile contract. |
| `PROFILE-OPERATION-FLUENCY` | Operation-fluency prompts, operations, and descriptors satisfy the common profile contract. |
| `PROFILE-OPERATION-EQUATION` | Missing-value/equation prompts and number-bond descriptors satisfy the common profile contract. |
| `PROFILE-GROUPING-MODEL` | Grouping, sharing, division, and remainder prompts and models satisfy the common profile contract. |
| `PROFILE-FRACTION-MODEL` | Fraction prompts and fraction-pair descriptors satisfy the common profile contract. |
| `PROFILE-DECIMAL-MODEL` | Decimal prompts and fractional/positional descriptors satisfy the common profile contract. |
| `PROFILE-MONEY-MODEL` | Money prompts and proportional models satisfy the common profile contract. MQ-048 validates the shipped sanitized descriptor by its `practiceMoney` kind, one original practice-token item, exact five candidates, and `practice-coins-v1` token-set version; it does not require the obsolete stripped `data.mode` UI hint. |
| `PROFILE-TIME-READ` | Clock-reading, duration, and timetable prompts and models satisfy the common profile contract. |
| `PROFILE-MEASURE-COMPARE` | Direct-measurement comparison prompts and models satisfy the common profile contract. |
| `PROFILE-MEASURE-INSTRUMENT` | Instrument, unit, perimeter, angle, and conversion prompts and models satisfy the common profile contract. |
| `PROFILE-GEOMETRY-CLASSIFY` | Shape and solid classification prompts and visual descriptors satisfy the common profile contract. |
| `PROFILE-GEOMETRY-TRANSFORM` | Route, symmetry, and transformation prompts and visual descriptors satisfy the common profile contract. |
| `PROFILE-DATA-DISPLAY` | Sorting and data-display prompts and descriptors satisfy the common profile contract. |
| `PROFILE-DATA-INVESTIGATE` | Survey and data-investigation prompts and descriptors satisfy the common profile contract. |
| `PROFILE-CHANCE-EXPERIMENT` | Chance-frequency and dependency prompts and descriptors satisfy the common profile contract. |
| `PROFILE-EXPRESSION-SOLVE` | Expression and variable-equation prompts and descriptors satisfy the common profile contract. |
| `PROFILE-ROUNDING-ESTIMATE` | Rounding and plausibility prompts and number-line descriptors satisfy the common profile contract. |
| `PROFILE-FACTOR-CLASSIFY` | Prime, composite, square, and cube classification prompts and descriptors satisfy the common profile contract. |
| `PROFILE-AREA-MODEL` | Rectangle and composite-area prompts and exact area-grid descriptors satisfy the common profile contract. |
| `PROFILE-VOLUME-MODEL` | Rectangular-prism volume prompts and unit-layer descriptors satisfy the common profile contract. |
| `VIS-CAPABILITIES` | Non-circular exact sets distinguish all 35 renderer-capable methods from the 33 methods reachable in shipped generated work. `NUMBER_BOND` and `NUMBER_CHOICE` are explicitly renderer-only; their impossible shipped-question combinations are excluded rather than mislabeled as missing coverage. Both answer classes and every reachable model family remain discoverable. |
| `VIS-CANADIAN-MONEY` | Money skills declare CAD and approved Canadian denominations; rendered prompts/models contain no foreign currency. |
| `VIS-LAB-CONTROLS` | Parent Test Lab renders the exact 33 release-reachable input methods with their real controls and performs no progress-storage write. The assertion compares against the fixed governed set rather than circularly accepting whatever generation happened to discover. |
| `VIS-LAB-MODELS` | Every semantically renderable prompt/model variant appears as a derived math model in Parent Test Lab. The semantic obligation key includes MQ-048's token id, and the exact model set must therefore contain one row for each of the five governed practice-token designs. The browser also inspects a 100-cell hundredths grid, a 10-part tenths strip, all four remainder captions and contracts, all eight volume representation/unit/solid combinations without answer leakage, and both exact timetable formats. |
| `VIS-DESKTOP-LAYOUT` | One case from every generator profile keeps the complete answer region, every visible control, and the grade control within 1366×768, 820×1180, 1180×820, and 1024×768 without horizontal overflow; checking only the answer's top edge cannot pass. The first and last enabled native controls must accept focus. At the exact 1024×768 profile, dense MQ-007 sort, MQ-105 area, and MQ-122 volume witnesses use a side-by-side model/response composition without hiding either region or reducing governed text and target floors. An additional exact MQ-048 matrix renders all five token/value fixtures in ordinary, Help, and real incorrect-feedback states at each of those four viewports (60 rows), requires five distinct computed visual fingerprints, keeps the source answer-free, names the value only in worked support, preserves 16 px text and 44 px control floors, and keeps the complete state-specific content on the first screen. Four separate first-use-guide rows require all five exact accessible mappings, no answer control, 50 px token floors, focused Ready, and the whole guide on the first screen. The child-feedback supplement creates correct PAIR_LINK and complete-but-wrong COUNT_TOUCH responses only through real controls, requires the outcome itself to receive focus, and keeps the subsequent action visible. The iPad-size rows are automated geometry/focus evidence only. A failure in either supplement is reported locally and cannot overwrite the independent lab-control or model results. |
| `VIS-MOBILE-LAYOUT` | One case from every manifest level is question-first, legible, non-overflowing, and actionable at 390×844 with 16 px text and 44 px control floors. The exact MQ-048 supplement renders all five token/value fixtures in ordinary, Help, and real incorrect-feedback states at phone width (15 rows), applies the same answer-free, worked-value, distinct-fingerprint, control-size, and first-screen-content contract, and submits each incorrect response through the real controls. One additional 390×844 first-use-guide row requires the exact five accessible mappings in the compact 3+2 layout, no answer control, focused Ready, 50 px token floors, and no first-screen or horizontal overflow. The child-feedback supplement exercises its legitimate correct/incorrect constructions, feedback-first focus, and visible-next contract without masking unrelated visual results. |

## Exhaustive generator proof

| Test | Protected effect |
|---|---|
| `GEN-EXHAUSTIVE` | All 126 skills generate and self-grade across both tiers, all three representations, all three themes, and 32 ordinals (72,576 questions total); 40,824 bounded question-choice searches preserve one mastery/input contract and expose only distinct reachable prompts. Selection contexts reject exploitable answer-location patterns by requiring sample-volume-appropriate position diversity and rejecting any position above a 75% share. |

## Publication and browser-evidence proof

`audit/tests/trusted-https-canary.test.mjs` protects the separate manual
GitHub-hosted Windows canary. Its closed evidence schema rejects reordered,
extra, noncanonical, stale-candidate, altered-Beta-1, changed-toolchain,
external-request, progress-loss, forged-green, and incomplete-teardown
records. Browser-argument mutants prove that certificate bypass and sandbox
weakening cannot be introduced; snapshot mutants bind every historical and
candidate path, byte count, and SHA-256; teardown failure injection proves all
cleanup actions still run in reverse order after one action fails. The runner
first observes and hashes at least one Edge process bound to the exact
disposable user-data directory, then requires the exact remaining process set
to be empty before deleting either that profile or its workspace; a lingering
process mutant keeps both paths for the wrapper's crash fallback. A closed
supply-chain validator and its fourteen mutants protect the exact Playwright
Test dependency closure, npm hardening flags, omitted optional dependencies,
suppressed browser downloads, Caddy URL and dual hashes, manual-only narrow
workflow, sanitized JSON artifact, loopback bindings, real shipped update and
recovery controls, and exclusion of every CI-only file/tool from the release
shell and service worker. This is focused technical evidence only: it neither
runs nor replaces the frozen-candidate certification gauntlet and does not
qualify the public host's privacy.
Both `audit/run-audit.ps1 -DevelopmentOnly` and the final release entry point
execute this focused module; only the separate manual hosted workflow can
produce a `RECONCILED` runtime observation.

`audit/tests/audit-orchestration.test.mjs` also recomputes the current
`docs/development/build-spec.md` SHA-256 and requires the public decision
register to bind that exact digest before complete certification. This moves a
stale-contract failure into the focused preflight instead of discovering it
only after the expensive browser and generator stages.

`audit/tests/publication-clearance.test.mjs` effect-tests the closed publication
schema and the candidate-only browser/runner review record. It proves that
changing any one of browser product, full product version, executable SHA-256,
`ImageOS`, or `ImageVersion` invalidates an otherwise valid approval; malformed,
missing, reordered, extra, or noncanonical records fail; the live executable
hash and requested hosted-runner label are bound; and a local run remains
explicitly local rather than claiming GitHub-hosted evidence.
It also proves that qualification review and final certification each require
an independently complete exact hosted tuple while permitting their values to
differ when the floating runner changes. A false publication flag, malformed
live tuple, pending reviewed record, or clearance-to-review mismatch still
fails closed; tuple equality is retained only as a diagnostic.
For Beta 6's exact two-file runtime-equivalent successor, it also independently
recomputes the qualification commit's public-payload SHA-256 and tree OID,
requires the clearance to bind those values, rejects either qualification
identity when mutated, and permits the successor's evidence-only payload
identity to differ as the governing policy requires.

The same suite now protects the eight first-class external release
branches:

For `EXT-CANARY`, browser request metadata is captured synchronously from the
Playwright request object so a delayed header observation cannot close the
shared browser during the update journey. The same header classifier runs on
the backend's raw received headers, which rejects cookies, authorization, API
keys, request bodies, credentials, and unsafe methods before serving bytes.
Focused mutants cover ordinary, cookie, authorization, API-key, and malformed
header observations; the runner guard forbids the former asynchronous
`allHeaders()` path.

When the backend rejects a runtime request, the failed canonical check records
only a closed diagnostic projection: a documented predicate bitmask (method,
path, query, credentials, body length, cookie, authorization, sensitive
header, and transfer encoding), an allowed public path when applicable,
otherwise a pathname digest, and classified method/body-length facts. Raw
query strings, unexpected paths, header values, credentials, and body content
never enter logs or evidence. An effect-sensitive mutant fills every unsafe
field with private-looking text, proves none survives the projection, and
proves the complete first finding fits the canonical check-detail bound.
The browser's exact automatic `/favicon.ico` probe is the only permitted
non-file path and remains a real 404; the Caddy/backend oracle compares each
request's observed status instead of assuming every allowed request returned
200. Near-name and all other unlisted paths remain violations.

| Test branch | Protected effect |
|---|---|
| `EXT-HOST` | A stable release requires exact `APPROVED` host/privacy qualification with a valid digest. An eligible prerelease may instead carry exact digest-bound `DEFERRED_PRERELEASE`, reported as `DEFERRED`, never PASS, approved, waived, or privacy-cleared. Stable-tag deferral, missing disclosure, pending, malformed, stale, future-dated, or candidate-mismatched evidence blocks. |
| `EXT-CANARY` | Beta 6 requires exact `RECONCILED` evidence with a canonical digest and reports PASS only after the trusted-HTTPS canary binds its qualification commit and runtime snapshot. Hosted-Windows listener observation retries only the transient `Get-NetTCPConnection` no-match result after Caddy's TCP socket opens; a wrong address, wrong owner process, or any other CIM error remains fail-closed. The disposable GitHub-hosted Windows runner must prove its administrator boundary before the exact Caddy root is installed and removed through bounded noninteractive `LocalMachine` Root `X509Store` operations; cleanup identity is persisted before installation and evidence normalizes its exact thumbprint. The leaf-certificate SHA-256 uses an effect-tested Windows PowerShell-compatible hashing path and fails closed on runtime errors. Caddy's SAN-only leaf may have no informational common-name value in Playwright, but a blank value is accepted only while the independent OS TLS probe verifies exact `localhost` DNS identity, the disposable Caddy issuer, a hashed leaf, and TLS 1.2 or 1.3; any nonblank browser subject must itself be `localhost`. Root scope is proven from the immutable Git manifest bytes, the rendered manifest-link target, and the live service-worker registration; page JavaScript never bypasses the product's `connect-src 'none'` privacy boundary to refetch the manifest. The synthetic Beta 1 witness submits the historical engine's exact `optionId` field and independently requires grading to identify the correct displayed option before it records evidence. Because that immutable save carries a retired curriculum digest, `RETIRED_BETA1_PRESERVED_FRESH_START` requires byte-identical source retention, an exact canonical fresh protected Beta 6 state with no transferred mastery/evidence/settings/logs/counts, the exact terminal retention marker, and a visible exact grown-up alert whose SHA-256 is bound into the closed evidence. The page never launches a competing registration update while an installing or waiting worker exists. Each worker also owns a nonce-bound staging transaction. The waiting-cache observation then requires the exact Beta 1 plus candidate physical-cache set, no staging or unknown cache, and an exact before-and-after cache snapshot within a bounded 15-second settle window before byte reconciliation. Together these checks prevent concurrent staging transactions or an in-progress install from impersonating a completed waiting worker. Because immutable Beta 1 predates the modern Web Locks writer lease, the update journey deliberately reloads the original Beta 1 page into Beta 6 after the same-origin backend switch; if the fresh-start notice opens another game screen, that same page deliberately uses its visible Home control before it checks and applies the update. The exact page then owns the modern writer lease and activates the visible Home update controls. A separate retained Beta 1 tab remains on its old document without being probed or navigated until the canary explicitly reloads it, at which point modern progress protection may apply. Installation-help checks separately enter the grown-up corner. The same-page candidate transition and moved-control paths are effect-tested and mutation-guarded. A failed run may truthfully report successful cleanup before any browser process was observed, while only `RECONCILED` evidence requires a nonzero observed exact-profile process set. Named checks emit START/PASS/FAIL markers, Beta 1 service-worker readiness and every native helper process are intrinsically bounded. Every renderer evaluation is bounded; timeout closes the exact persistent browser, terminates its disposable-profile process tree when needed, and drains the evaluation before shared handles or teardown evidence advance. The detached-response check uses an auxiliary context in that same bounded browser rather than a second process. The online-to-offline boundary uses bounded browser close with verified process absence, bounded Caddy exit, and forced backend connection shutdown. Timeout observation never represents cancellation. Beta 4's historical `OWNER_SKIPPED_BETA4`/`NONE` state remains visible, non-passing, expired, and unable to impersonate reconciliation, evidence, host approval, or privacy clearance. State, digest, snapshot, candidate, and tag mutants prevent `Shippable: YES`. |
| `EXT-DEVICE` | The six-lane device cycle is offered but optional. A declined cycle must use exact `OPTIONAL_NOT_RUN`/`NONE`/zero/`NOT_RUN` fields and is reported as `OPTIONAL`, never `PASS`; if selected, all six lanes and the primary-iPad journey must pass under one digest-bound `COMPLETE` record. Partial, inferred, emulator-only, malformed, or concealed states block. |
| `EXT-REVIEWERS` | The six-reviewer cycle is offered but optional. A declined cycle must use exact `OPTIONAL_NOT_RUN`/`NONE`/zero fields and is reported as `OPTIONAL`, never `PASS`; if selected, all six context-independent reports must be sealed under one digest-bound `COMPLETE` record. Partial, malformed, or concealed review evidence blocks. |
| `EXT-ADJUDICATION` | Independent adjudication must be `APPROVED`, recommend `RELEASE`, and carry its exact digest. Pending, missing, contrary, or malformed adjudication blocks. |
| `EXT-FINDINGS` | Disposition must be `COMPLETE`, with zero open Critical/High, zero unaccepted Medium, and zero unrecorded Low findings. Each nonzero-count mutant blocks independently. |
| `EXT-HOSTED-WINDOWS` | The state must be `REVIEWED`, and its digest and clearance fields must equal the canonical qualification browser/runner evidence bytes. The final audit independently requires a complete exact GitHub-hosted tuple; because `windows-latest` floats, equality between the two valid tuples is diagnostic rather than a gate. Local, malformed, pending, stale, falsely labelled, or digest-mismatched evidence blocks. |
| `EXT-OWNER` | Only exact project-owner `PR_PUSH_AUTHORIZED` evidence, a valid review-bundle digest, tag `v1.0.0-beta.6`, and protected ref `refs/heads/main` can pass. Missing, reordered, duplicated, blocked, wrong-tag, or wrong-ref decision inputs cannot compute a ship decision. |

The suite preserves the historical Beta 4 effect test for
`DIRECT_EVIDENCE_SUCCESSOR_V1`: the final Beta 4
candidate must be the single-parent immediate child of the named qualification
commit; its exact diff contains only `PUBLICATION_CLEARANCE.md` and
`audit/browser-runner-evidence-v1.json`; and both predecessor records are
pending. Merge, wrong-parent, skipped-ancestor, missing-path, extra-doc,
game-byte, and already-approved predecessor mutants all fail closed. A real
temporary Git repository exercises the production Git observer across two
actual commits, while the workflow regression requires the release checkout to
fetch full parent history so the hosted gauntlet can prove the same relation.

For Beta 6, the suite instead effect-tests
`RUNTIME_EQUIVALENT_EVIDENCE_SUCCESSOR_V1`. The protected-main qualification
commit contains final game/runtime/PWA bytes and pending clearance/browser
evidence; its final successor is the immediate non-merge sole child, and the
exact diff contains only `PUBLICATION_CLEARANCE.md` and
`audit/browser-runner-evidence-v1.json`. The reconciled canary binds the
qualification commit and its runtime snapshot, while a separate identity
validator proves every governed runtime/PWA byte is identical in the
successor. Wrong-parent, merge, skipped-ancestor, missing/extra path,
runtime-byte, PWA-byte, snapshot, or qualification-commit mutants fail closed.
The public-payload digest must differ because reviewed browser evidence is
public; a forged whole-payload-equality claim also fails.
The standalone Pages validator must pass the observer's exact qualification
payload SHA-256 and tree OID into the same clearance matcher; omitting either
identity is an effect-tested failure rather than a late deployment surprise.

`audit/run-audit.mjs` predicts and observes all eight records separately. The
16 direct Playwright journeys raise the closed technical-plus-external total
from 267 to 283. The decision helper requires the
exact eight IDs in exact order. For Beta 6 it requires five mandatory results
`PASS`, including exact digest-bound `EXT-CANARY: PASS` from `RECONCILED`
evidence; exact evaluator-bound `EXT-HOST: DEFERRED`; both optional results as
exact `OPTIONAL_NOT_RUN`/`OPTIONAL` under PB-049; external aggregate `PASS`;
publication `APPROVED`; and technical gates true. A fully approved prerelease
host may still PASS. The historical Beta 4 branch separately preserves its
four mandatory PASS results and visible non-passing owner-skipped canary. A
stable release requires all six ordinarily mandatory results to PASS. Tests
delete, duplicate, reorder, block, partially complete, forge optional,
DEFERRED, or OWNER_SKIPPED states, mutate the host digest/disclosure, retain a pending canary,
and evaluate the deferral under a stable tag to prove none can produce
`Shippable: YES`. All unresolved mandatory, stable-deferred, or malformed
selected-optional results remain fail-closed; no assertion was removed or
weakened.

For emergency Beta 3 only, an additional effect-sensitive exception regression
requires the first six gates to remain visibly `WAIVED` (never `PASS`), with
their zero/unknown counts and one exact owner-waiver digest.
`EXT-HOSTED-WINDOWS` must still pass, and `EXT-OWNER` must use
`EMERGENCY_BETA3_AUTHORIZED`. A wrong tag, ref, status, count, digest,
residual-risk statement, technical result, or gate ordering blocks release.

## Representative mutation proof

`audit/mutation-runner.mjs` creates disposable copies under the operating
system's temporary directory. It alters one effect in each required family and
runs only the protecting test. The mastery/promotion family also contains a
nested task-type-coverage mutant. Three dedicated strategy mutants remove
intermediate-work completeness, trust hidden `params.strategy`, or trust
`question.answer`; `BEH-07` kills each through its independent visible-contract
oracle. The eleven families therefore kill twelve effect-sensitive cases. A family passes only if its baseline test passes and
all of its mutants fail. The task-type probe supplies the shipped construction
witness count, current-contract distinct sample keys, and an applied witness
when the selected multi-task skill requires it; the baseline must remain
`PRACTISING` while the deliberately weakened `every`-to-`some` mutant becomes
`SOLID`. This prevents a shared unmet mastery condition from letting both
versions remain `PRACTISING` and falsely appear protected. The shipped
`index.html` is never edited.
